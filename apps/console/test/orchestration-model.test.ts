import assert from "node:assert/strict";import test from "node:test";import { buildOrchestrationSnapshot,projectOverview } from "../src/features/orchestration/model.ts";
test("orchestration snapshot merges sources and deduplicates conductor A2A mirrors",()=>{const now=Date.parse("2026-09-10T10:00:00Z");const snap=buildOrchestrationSnapshot({cloudTasks:[{id:"cloud-1",status:"running"}],conductorTasks:[{id:"c-1",status:"running"}],a2aTasks:[{id:"a-1",state:"working",metadata:{conductor:{task_id:"c-1"}}}],combos:[{id:"combo-1",name:"fallback"}],sources:[{source:"cloud-agent",ok:true},{source:"a2a",ok:true},{source:"conductor",ok:true},{source:"routing",ok:true}]},now);assert.equal(snap.nodes.some(x=>x.id==="a2a:a-1"),false);assert.equal(snap.edges.some(x=>x.kind==="mirror"),true);assert.equal(projectOverview(snap).running,2);});
test("orchestration snapshot hides old terminal tasks unless requested",()=>{const input={a2aTasks:[{id:"old",state:"completed",updatedAt:"2026-09-10T09:00:00Z"}],sources:[{source:"a2a" as const,ok:true}]};assert.equal(buildOrchestrationSnapshot(input,Date.parse("2026-09-10T10:00:00Z")).nodes.some(x=>x.id==="a2a:old"),false);assert.equal(buildOrchestrationSnapshot(input,Date.parse("2026-09-10T10:00:00Z"),true).nodes.some(x=>x.id==="a2a:old"),true);});
