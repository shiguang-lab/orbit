{
  "name": "@shiguang-gateway/importer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "import": "tsx src/main.ts",
    "infra:migrate-postgres": "node src/migrate-sqlite-to-postgres.mjs",
    "build": "tsc -p tsconfig.json --pretty false",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "tsx": "^4.23.13"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
