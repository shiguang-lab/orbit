name: Publish Orbit images

on:
  push:
    branches: [main]
    tags: ["v*", "v*-*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Image tag to publish (for example 0.1.0)
        required: false
        default: latest
        type: string

permissions:
  contents: read

concurrency:
  group: orbit-publish-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_PREFIX: ghcr.io/shiguang-lab/orbit

jobs:
  verify:
    name: Verify independent runtime
    runs-on: ubuntu-latest
    env:
      # Release validation must not silently depend on a sibling official
      # checkout; the route/HTTP contract hashes are committed in this repo.
      ORBIT_REFERENCE_DIR: /tmp/no-official-orbit
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck and build
        run: |
          pnpm typecheck
          pnpm build

      - name: Run independence and runtime smoke gates
        run: |
          pnpm audit:gateway-independence
          pnpm audit:route-contracts
          pnpm audit:brand
          pnpm --filter @orbit/console test
          pnpm --filter @orbit/auth test
          pnpm --filter @orbit/realtime test
          pnpm --filter @orbit/utils test
          pnpm --filter @orbit/importer test
          pnpm --filter @orbit/http test
          pnpm --filter @orbit/control exec node --import tsx --test ../../packages/http/test/web-request.test.ts
          pnpm --filter @orbit/control exec node --import tsx --test test/settings-import-request.test.ts
          pnpm --filter @orbit/control exec node --import tsx --test test/auth-session.test.ts test/combo-runtime-registration.test.ts
          pnpm --filter @orbit/gateway exec tsx --test test/dispatcher-module-injection.test.ts
          pnpm smoke:route-imports
          pnpm smoke:split-deployment
          node --test scripts/smoke-worker-start.test.mjs
          node --test scripts/compose-auth-contract.test.mjs
          pnpm smoke:worker
          pnpm smoke:agent-skills

  publish:
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: console
            image: console
          - target: gateway
            image: gateway
          - target: control
            image: control
          - target: realtime
            image: realtime
          - target: worker
            image: worker
          - target: importer
            image: importer
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Reclaim runner disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc
          docker system prune --all --force

      - name: Resolve image tags
        id: meta
        shell: bash
        env:
          IMAGE_NAME: ${{ env.IMAGE_PREFIX }}-${{ matrix.image }}
          EVENT_NAME: ${{ github.event_name }}
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" == workflow_dispatch ]]; then
            primary="${INPUT_TAG:-latest}"
          elif [[ "$REF_TYPE" == tag ]]; then
            primary="${REF_NAME#v}"
          else
            primary="latest"
          fi
          {
            echo "tags<<EOF"
            echo "$IMAGE_NAME:$primary"
            echo "$IMAGE_NAME:sha-${GITHUB_SHA::12}"
            if [[ "$REF_TYPE" == tag ]]; then echo "$IMAGE_NAME:$REF_NAME"; fi
            if [[ "$primary" != latest && "$EVENT_NAME" != workflow_dispatch ]]; then echo "$IMAGE_NAME:latest"; fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build ${{ matrix.target }} image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: ${{ matrix.target }}
          # NAS deployment target is x86_64; keeping one platform avoids
          # exhausting GitHub-hosted runner disk while building six services.
          platforms: linux/amd64
          push: ${{ !contains(fromJSON('["gateway", "control", "realtime", "worker"]'), matrix.target) }}
          load: ${{ contains(fromJSON('["gateway", "control", "realtime", "worker"]'), matrix.target) }}
          provenance: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            org.opencontainers.image.title=Orbit
            org.opencontainers.image.source=https://github.com/shiguang-lab/orbit
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=orbit-${{ matrix.target }}
          # Export only the final cache chain. mode=max duplicated every large
          # pnpm/Docker layer and exhausted the hosted runner disk.
          cache-to: type=gha,scope=orbit-${{ matrix.target }},mode=min

      - name: Start built runtime image before publishing
        if: contains(fromJSON('["gateway", "control", "realtime", "worker"]'), matrix.target)
        env:
          TARGET: ${{ matrix.target }}
          IMAGE_TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          first_tag="${IMAGE_TAGS%%$'\n'*}"
          node scripts/smoke-built-image.mjs "$TARGET" "$first_tag"

      - name: Push verified local image tags
        if: contains(fromJSON('["gateway", "control", "realtime", "worker"]'), matrix.target)
        env:
          IMAGE_TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          set -euo pipefail
          while IFS= read -r tag; do
            if [[ -n "$tag" ]]; then docker push "$tag"; fi
          done <<< "$IMAGE_TAGS"

  release:
    name: Build GitHub Release assets
    needs: publish
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build source and NAS deployment archives
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          mkdir -p release-assets/nas-deploy
          git archive --format=tar.gz --prefix="orbit-${VERSION}/" HEAD \
            -o "release-assets/orbit-${VERSION}.source.tar.gz"
          git archive --format=zip --prefix="orbit-${VERSION}/" HEAD \
            -o "release-assets/orbit-${VERSION}.source.zip"
          cp docker-compose.yml .env.example deploy/NAS-DEPLOY.md release-assets/nas-deploy/
          mkdir -p release-assets/nas-deploy/deploy/postgres-init
          cp deploy/docker-compose.infrastructure.yml deploy/.env.infrastructure.example release-assets/nas-deploy/deploy/
          cp deploy/postgres-init/*.sql release-assets/nas-deploy/deploy/postgres-init/
          tar -czf "release-assets/orbit-${VERSION}-nas-deploy.tar.gz" \
            -C release-assets/nas-deploy .
          (cd release-assets && sha256sum orbit-*.source.tar.gz orbit-*.source.zip orbit-*-nas-deploy.tar.gz > SHA256SUMS.txt)

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          body: |
            ## Orbit

            Container images (one per deployable service):

            `ghcr.io/shiguang-lab/orbit-console:${{ github.ref_name }}`
            `ghcr.io/shiguang-lab/orbit-gateway:${{ github.ref_name }}`
            `ghcr.io/shiguang-lab/orbit-control:${{ github.ref_name }}`
            `ghcr.io/shiguang-lab/orbit-realtime:${{ github.ref_name }}`
            `ghcr.io/shiguang-lab/orbit-worker:${{ github.ref_name }}`
            `ghcr.io/shiguang-lab/orbit-importer:${{ github.ref_name }}`

            NAS deployment downloads the `-nas-deploy.tar.gz` asset, sets all six
            `ORBIT_*_IMAGE` variables and the stable encryption/auth secrets,
            then runs `docker compose pull && docker compose up -d`.
          files: |
            release-assets/orbit-${{ github.ref_name }}.source.tar.gz
            release-assets/orbit-${{ github.ref_name }}.source.zip
            release-assets/orbit-${{ github.ref_name }}-nas-deploy.tar.gz
            release-assets/SHA256SUMS.txt
          fail_on_unmatched_files: true
