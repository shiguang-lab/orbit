# PostgreSQL/Redis are deployed independently from the application containers.
# Keep this file outside source control after filling the secret.
POSTGRES_DB=orbit
POSTGRES_USER=orbit
POSTGRES_PASSWORD=replace-with-a-stable-random-secret
POSTGRES_HOST_PORT=25432
REDIS_HOST_PORT=26379
INFRA_BIND_ADDRESS=127.0.0.1
